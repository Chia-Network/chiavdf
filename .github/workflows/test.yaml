name: Test Prover and vdf_client

on:
  push:
    branches:
      - main
  release:
    types: [published]
  pull_request:
    branches:
      - '**'

jobs:
  test:
    name: Test ${{ matrix.config }} ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    env:
      CHIAVDF_LOG_AVX: "1"
    strategy:
      fail-fast: false
      matrix:
        os: [macos-13-intel, macos-13-arm64, ubuntu-latest, windows-latest]
        config: [optimized=1, TSAN=1, ASAN=1]
        exclude:
          # clang-cl TSAN is unsupported for x86_64-pc-windows-msvc, so
          # Windows TSAN fails during CMake compiler checks.
          - os: windows-latest
            config: TSAN=1

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    # See: https://github.com/google/sanitizers/issues/1716
    # Fixes `FATAL: ThreadSanitizer: unexpected memory mapping 0x70498d8ae000-0x70498dd00000` type errors
    - name: Adjust mmap_rnd_bits on ubuntu
      if: startsWith(matrix.os, 'ubuntu')
      run: |
        sudo sysctl vm.mmap_rnd_bits=28

    - name: Build vdf-client on Ubuntu
      if: startsWith(matrix.os, 'ubuntu')
      run: |
        # Avoid transient 404s from stale apt indices / mirror lag.
        sudo apt-get update -y -o Acquire::Retries=3
        sudo apt-get install libgmp-dev libboost-python-dev libpython3-dev libboost-system-dev build-essential -y
        cd src
        make ${{ matrix.config }} -f Makefile.vdf-client

    - name: Build vdf-client on Mac
      if: startsWith(matrix.os, 'mac')
      run: |
        brew ls --versions boost >/dev/null 2>&1 || brew install boost
        BOOST_VERSION="$(brew list --versions boost | awk '{print $2}')"
        BOOST_INCLUDE=""
        for cand in \
          "$(brew --prefix boost)/include" \
          "$(brew --prefix)/include" \
          "$(brew --cellar boost)/${BOOST_VERSION}/include"; do
          if [ -f "$cand/boost/asio.hpp" ]; then
            BOOST_INCLUDE="$cand"
            break
          fi
        done
        if [ -z "$BOOST_INCLUDE" ]; then
          echo "Could not locate boost/asio.hpp for brew boost=${BOOST_VERSION}" >&2
          brew --prefix boost || true
          brew --cellar boost || true
          brew info boost || true
          exit 1
        fi
        cd src
        make ${{ matrix.config }} CPPFLAGS="-I${BOOST_INCLUDE} ${CPPFLAGS:-}" -f Makefile.vdf-client

    - name: Install LLVM and Ninja on Windows
      if: startsWith(matrix.os, 'windows')
      shell: pwsh
      run: |
        choco install llvm ninja boost-msvc-14.3 -y
        "C:\Program Files\LLVM\bin" | Out-File -Append -FilePath $env:GITHUB_PATH
        "CHIA_ENABLE_AVX512_IFMA=0" | Out-File -Append -FilePath $env:GITHUB_ENV
        $boostDir = Get-ChildItem "C:\local" -Directory -Filter "boost_*" | Select-Object -First 1
        if (-not $boostDir) {
          throw "Boost install not found under C:\local"
        }
        "BOOST_INCLUDE_DIR=$($boostDir.FullName)" | Out-File -Append -FilePath $env:GITHUB_ENV

    - name: Checkout mpir for Windows
      if: startsWith(matrix.os, 'windows')
      uses: actions/checkout@v6
      with:
        repository: Chia-Network/mpir_gc_x64
        fetch-depth: 1
        path: mpir_gc_x64

    - name: Build vdf-client on Windows
      if: startsWith(matrix.os, 'windows')
      shell: pwsh
      run: |
<<<<<<< HEAD
        cmake -S src -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=clang-cl -DCMAKE_CXX_COMPILER=clang-cl -DCMAKE_ASM_COMPILER=clang-cl -DBOOST_INCLUDE_DIR="$env:BOOST_INCLUDE_DIR" -DBUILD_PYTHON=OFF -DBUILD_CHIAVDFC=OFF -DBUILD_VDF_CLIENT=ON -DBUILD_VDF_BENCH=ON -DBUILD_VDF_TESTS=ON -DBUILD_HW_TOOLS=OFF
=======
        $cmakeBuildType = "Release"
        $cmakeCFlags = ""
        $cmakeCxxFlags = ""
        $cmakeExeLinkerFlags = ""
        $cmakeSharedLinkerFlags = ""
        $cmakeModuleLinkerFlags = ""
        if ("${{ matrix.config }}" -eq "ASAN=1") {
          $cmakeBuildType = "RelWithDebInfo"
          # clang-cl on Windows expects /fsanitize=address, and lld-link does
          # not understand -fsanitize=address linker flags. CMake links probes
          # with lld-link directly, so add ASAN runtimes explicitly.
          $cmakeCFlags = "/fsanitize=address"
          $cmakeCxxFlags = "/fsanitize=address"
          $clangResourceDir = (& clang-cl --print-resource-dir).Trim()
          $asanLibDir = Join-Path $clangResourceDir "lib\windows"
          if (-not (Test-Path $asanLibDir)) {
            throw "ASAN runtime library directory not found: $asanLibDir"
          }
          $asanRuntimeLib = Join-Path $asanLibDir "clang_rt.asan_dynamic-x86_64.lib"
          $asanThunkLib = Join-Path $asanLibDir "clang_rt.asan_dynamic_runtime_thunk-x86_64.lib"
          if (-not (Test-Path $asanRuntimeLib)) {
            throw "ASAN runtime library not found: $asanRuntimeLib"
          }
          if (-not (Test-Path $asanThunkLib)) {
            throw "ASAN runtime thunk library not found: $asanThunkLib"
          }
          $asanLinkLibs = "clang_rt.asan_dynamic_runtime_thunk-x86_64.lib clang_rt.asan_dynamic-x86_64.lib"
          $cmakeExeLinkerFlags = $asanLinkLibs
          $cmakeSharedLinkerFlags = $asanLinkLibs
          $cmakeModuleLinkerFlags = $asanLinkLibs
          if ($env:LIB) {
            $env:LIB = "$asanLibDir;$env:LIB"
          } else {
            $env:LIB = "$asanLibDir"
          }
        } elseif ("${{ matrix.config }}" -eq "TSAN=1") {
          $cmakeBuildType = "RelWithDebInfo"
          # Keep sanitizer flag on compile side for clang-cl.
          $cmakeCFlags = "/fsanitize=thread"
          $cmakeCxxFlags = "/fsanitize=thread"
          $clangResourceDir = (& clang-cl --print-resource-dir).Trim()
          $tsanLibDir = Join-Path $clangResourceDir "lib\windows"
          if (-not (Test-Path $tsanLibDir)) {
            throw "TSAN runtime library directory not found: $tsanLibDir"
          }
          if ($env:LIB) {
            $env:LIB = "$tsanLibDir;$env:LIB"
          } else {
            $env:LIB = "$tsanLibDir"
          }
        }
        # Force compiler probes (try_compile) to use release CRT when
        # sanitizers are enabled; clang-cl rejects /MDd + -fsanitize=address.
        $cmakeArgs = @(
          "-S", "src",
          "-B", "build",
          "-G", "Ninja",
          "-DCMAKE_BUILD_TYPE=$cmakeBuildType",
          "-DCMAKE_TRY_COMPILE_CONFIGURATION=Release",
          "-DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreadedDLL",
          "-DCMAKE_C_COMPILER=clang-cl",
          "-DCMAKE_CXX_COMPILER=clang-cl",
          "-DCMAKE_ASM_COMPILER=clang-cl",
          "-DCMAKE_C_FLAGS=$cmakeCFlags",
          "-DCMAKE_CXX_FLAGS=$cmakeCxxFlags",
          "-DCMAKE_EXE_LINKER_FLAGS=$cmakeExeLinkerFlags",
          "-DCMAKE_SHARED_LINKER_FLAGS=$cmakeSharedLinkerFlags",
          "-DCMAKE_MODULE_LINKER_FLAGS=$cmakeModuleLinkerFlags",
          "-DBOOST_INCLUDE_DIR=$env:BOOST_INCLUDE_DIR",
          "-DBUILD_PYTHON=OFF",
          "-DBUILD_CHIAVDFC=OFF",
          "-DBUILD_VDF_CLIENT=ON",
          "-DBUILD_VDF_BENCH=ON",
          "-DBUILD_VDF_TESTS=ON",
          "-DBUILD_HW_TOOLS=OFF"
        )
        & cmake @cmakeArgs
>>>>>>> 880d11f (more cmake instruction issues)
        cmake --build build --target vdf_client vdf_bench 1weso_test 2weso_test prover_test

    - name: Run vdf tests (optimized)
      if: matrix.config == 'optimized=1' && !startsWith(matrix.os, 'windows')
      run: |
        cd src
        echo "Running 1weso_test"
        ./1weso_test
        echo "Running 2weso_test"
        ./2weso_test
        echo "Running prover_test"
        if [[ "${{ matrix.os }}" == ubuntu* ]]; then
          ./prover_test
        else
          CHIAVDF_PROVER_TEST_FAST=1 ./prover_test
        fi

    - name: Run vdf tests (short)
      if: matrix.config != 'optimized=1' && !startsWith(matrix.os, 'windows')
      run: |
        cd src
        echo "Running 1weso_test"
        ./1weso_test 1000
        echo "Running 2weso_test"
        ./2weso_test 1000

    # macos thread sanitizer will give false positives on ./prover_test because
    # it prints to std::cout from multiple threads. Which is allowed by the
    # standard
    - name: test Prover
      if: matrix.config != 'optimized=1' && (matrix.config != 'TSAN=1' || startsWith(matrix.os, 'ubuntu')) && !startsWith(matrix.os, 'windows')
      run: |
        cd src
        echo "Running prover_test"
        if [[ "${{ matrix.os }}" == ubuntu* ]]; then
          ./prover_test
        else
          CHIAVDF_PROVER_TEST_FAST=1 ./prover_test
        fi

    - name: Test vdf-client (Windows)
      if: matrix.config == 'optimized=1' && startsWith(matrix.os, 'windows')
      shell: pwsh
      run: |
        cd build
        $dllPaths = @()
        if ($env:MPIR_ROOT -and (Test-Path "$env:MPIR_ROOT\bin")) { $dllPaths += "$env:MPIR_ROOT\bin" }
        if (Test-Path "$env:GITHUB_WORKSPACE\mpir_gc_x64") { $dllPaths += "$env:GITHUB_WORKSPACE\mpir_gc_x64" }
        if ($dllPaths.Count -gt 0) { $env:PATH = ($dllPaths -join ';') + ';' + $env:PATH }
        Write-Host "Running 1weso_test"
        .\1weso_test.exe
        Write-Host "Running 2weso_test"
        .\2weso_test.exe
        Write-Host "Running prover_test"
        $env:CHIAVDF_PROVER_TEST_FAST = "1"
        .\prover_test.exe

    - name: Benchmark vdf_bench square (Ubuntu/Mac)
      if: "!startsWith(matrix.os, 'windows')"
      run: |
        cd src
        echo "Benchmarking vdf_bench with 2,000,000 iterations of square_asm"
        ./vdf_bench square_asm 2000000

    - name: Benchmark vdf_bench square (Windows)
      if: startsWith(matrix.os, 'windows')
      shell: pwsh
      run: |
        cd build
        $dllPaths = @()
        if ($env:MPIR_ROOT -and (Test-Path "$env:MPIR_ROOT\bin")) { $dllPaths += "$env:MPIR_ROOT\bin" }
        if (Test-Path "$env:GITHUB_WORKSPACE\mpir_gc_x64") { $dllPaths += "$env:GITHUB_WORKSPACE\mpir_gc_x64" }
        if ($dllPaths.Count -gt 0) { $env:PATH = ($dllPaths -join ';') + ';' + $env:PATH }
        Write-Host "Benchmarking vdf_bench with 2,000,000 iterations of square_asm"
        & .\vdf_bench.exe square_asm 2000000
        Write-Host "vdf_bench exit code: $LASTEXITCODE"
        if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

